

===== src/components/page-header.tsx =====
export function PageHeader({
  eyebrow,
  title,
  intro,
}: {
  eyebrow: string;
  title: string;
  intro?: string;
}) {
  return (
    <section className="relative overflow-hidden border-b border-border">
      <div className="pointer-events-none absolute inset-0 blueprint-grid opacity-40" />
      <div className="pointer-events-none absolute inset-0 bg-gradient-to-b from-transparent to-background" />
      <div className="relative mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-20">
        <p className="tech-label reveal text-primary">{eyebrow}</p>
        <h1 className="reveal mt-4 max-w-3xl text-4xl font-bold leading-[1.05] sm:text-5xl lg:text-6xl">
          {title}
        </h1>
        <div className="reveal mt-6 h-px w-20 bg-primary" />
        {intro && (
          <p className="reveal mt-6 max-w-2xl text-base leading-relaxed text-muted-foreground">
            {intro}
          </p>
        )}
      </div>
    </section>
  );
}


===== src/components/site-footer.tsx =====
import { Link } from "@tanstack/react-router";
import { Github, Linkedin, Mail } from "lucide-react";

export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-surface">
      <div className="mx-auto flex max-w-7xl flex-col gap-6 px-5 py-10 sm:px-8 md:flex-row md:items-center md:justify-between">
        <div>
          <p className="font-display text-lg font-bold tracking-tight">BRIARA BROUSSARD</p>
          <p className="mt-1 text-sm text-muted-foreground">
            Mechanical Engineering Technology · University of Houston
          </p>
        </div>

        <div className="flex items-center gap-3">
          <SocialLink href="https://www.linkedin.com" label="LinkedIn">
            <Linkedin className="h-4 w-4" />
          </SocialLink>
          <SocialLink href="https://github.com" label="GitHub">
            <Github className="h-4 w-4" />
          </SocialLink>
          <SocialLink href="mailto:Broussardbriara@gmail.com" label="Email">
            <Mail className="h-4 w-4" />
          </SocialLink>
        </div>
      </div>
      <div className="border-t border-border/70">
        <div className="mx-auto flex max-w-7xl flex-col gap-2 px-5 py-4 text-xs text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-8">
          <span>© {new Date().getFullYear()} Briara Broussard. All rights reserved.</span>
          <Link to="/contact" className="tech-label text-muted-foreground hover:text-primary">
            Get in touch
          </Link>
        </div>
      </div>
    </footer>
  );
}

function SocialLink({
  href,
  label,
  children,
}: {
  href: string;
  label: string;
  children: React.ReactNode;
}) {
  return (
    <a
      href={href}
      target="_blank"
      rel="noreferrer noopener"
      aria-label={label}
      className="grid h-10 w-10 place-items-center border border-border text-muted-foreground transition-colors hover:border-primary hover:text-primary"
    >
      {children}
    </a>
  );
}


===== src/components/site-nav.tsx =====
import { Link } from "@tanstack/react-router";
import { Menu, X } from "lucide-react";
import { useState } from "react";

const links = [
  { to: "/", label: "HOME" },
  { to: "/about", label: "ABOUT" },
  { to: "/projects", label: "PROJECTS" },
  { to: "/skills", label: "SKILLS" },
  { to: "/experience", label: "EXPERIENCE" },
  { to: "/resume", label: "RESUME" },
  { to: "/contact", label: "CONTACT" },
] as const;

export function SiteNav() {
  const [open, setOpen] = useState(false);

  return (
    <header className="sticky top-0 z-50 border-b border-border bg-background/85 backdrop-blur-md">
      <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 sm:px-8">
        <Link to="/" className="group flex items-center gap-3" onClick={() => setOpen(false)}>
          <span className="grid h-9 w-9 place-items-center border border-primary/60 bg-primary/10 font-display text-sm font-bold tracking-tight text-primary transition-colors group-hover:bg-primary group-hover:text-primary-foreground">
            BB
          </span>
          <span className="font-display text-sm font-bold tracking-[0.14em] text-foreground sm:text-base">
            BRIARA BROUSSARD
          </span>
        </Link>

        <nav className="hidden items-center gap-1 lg:flex">
          {links.map((l) => (
            <Link
              key={l.to}
              to={l.to}
              activeOptions={{ exact: l.to === "/" }}
              className="tech-label relative px-3 py-2 text-muted-foreground transition-colors hover:text-foreground data-[status=active]:text-primary"
            >
              {l.label}
            </Link>
          ))}
        </nav>

        <button
          type="button"
          aria-label={open ? "Close menu" : "Open menu"}
          aria-expanded={open}
          onClick={() => setOpen((v) => !v)}
          className="grid h-10 w-10 place-items-center border border-border text-foreground transition-colors hover:border-primary hover:text-primary lg:hidden"
        >
          {open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}
        </button>
      </div>

      {open && (
        <nav className="border-t border-border bg-surface lg:hidden">
          <div className="mx-auto flex max-w-7xl flex-col px-5 py-2 sm:px-8">
            {links.map((l) => (
              <Link
                key={l.to}
                to={l.to}
                activeOptions={{ exact: l.to === "/" }}
                onClick={() => setOpen(false)}
                className="tech-label border-b border-border/60 py-4 text-muted-foreground transition-colors last:border-0 hover:text-foreground data-[status=active]:text-primary"
              >
                {l.label}
              </Link>
            ))}
          </div>
        </nav>
      )}
    </header>
  );
}


===== src/data/projects.ts =====
import solidworksImg from "@/assets/project-solidworks.jpg";
import drawingImg from "@/assets/project-drawing.jpg";
import roboticsImg from "@/assets/project-robotics.jpg";

export type Project = {
  slug: string;
  index: string;
  title: string;
  category: string;
  summary: string;
  image: string;
  tags: string[];
  software: string[];
  skills: string[];
  status: string;
  overview: string;
  objective: string;
  role: string;
  process: { step: string; detail: string }[];
  result: string;
  lessons: string[];
  gallery: { caption: string }[];
};

/**
 * Placeholder project case studies. Update the copy and swap the imported
 * image to publish a real project — the routes and cards read from here.
 */
export const projects: Project[] = [
  {
    slug: "solidworks-project",
    index: "01",
    title: "SolidWorks Project",
    category: "CAD / Part Design",
    summary:
      "Placeholder for a parametric part and assembly modeled in SolidWorks, documented from concept sketch through final render.",
    image: solidworksImg,
    tags: ["SOLIDWORKS", "CAD", "3D MODELING"],
    software: ["SolidWorks", "SolidProfessor"],
    skills: ["3D Modeling", "Parametric Design", "Design Intent", "Assemblies"],
    status: "Placeholder — details to be added",
    overview:
      "Project details coming soon. This case study will cover a mechanical component modeled in SolidWorks, including sketch strategy, feature tree organization, and assembly mates.",
    objective:
      "To be added: the design requirement, constraints, and target function of the part.",
    role: "To be added: Briara's responsibilities on the project, from concept sketches to final modeling and documentation.",
    process: [
      { step: "Research & Requirements", detail: "To be added." },
      { step: "Concept Sketching", detail: "To be added." },
      { step: "3D Modeling", detail: "To be added." },
      { step: "Review & Iteration", detail: "To be added." },
    ],
    result: "Final outcome and deliverables to be added.",
    lessons: [
      "Lessons learned to be added once the project is documented.",
    ],
    gallery: [
      { caption: "CAD screenshot — placeholder" },
      { caption: "Isometric view — placeholder" },
    ],
  },
  {
    slug: "technical-drawing",
    index: "02",
    title: "Technical Drawing",
    category: "Engineering Graphics",
    summary:
      "Placeholder for a fully dimensioned multi-view engineering drawing produced to standard drafting practice.",
    image: drawingImg,
    tags: ["AUTOCAD", "ENGINEERING GRAPHICS", "TECHNICAL DRAWING"],
    software: ["AutoCAD"],
    skills: [
      "Orthographic Projection",
      "Dimensioning & Tolerancing",
      "Drafting Standards",
      "Technical Communication",
    ],
    status: "Placeholder — details to be added",
    overview:
      "Project details coming soon. This case study will present a multi-view drawing set with dimensions, section views, and a completed title block.",
    objective: "To be added: the drawing package requirements and standards followed.",
    role: "To be added: Briara's drafting responsibilities and review process.",
    process: [
      { step: "View Selection", detail: "To be added." },
      { step: "Layout & Scale", detail: "To be added." },
      { step: "Dimensioning", detail: "To be added." },
      { step: "Drawing Check", detail: "To be added." },
    ],
    result: "Final drawing package details to be added.",
    lessons: ["Lessons learned to be added once the project is documented."],
    gallery: [
      { caption: "Sheet 1 — placeholder" },
      { caption: "Detail view — placeholder" },
    ],
  },
  {
    slug: "robotics-hands-on",
    index: "03",
    title: "Robotics / Hands-On Project",
    category: "Build / Mechatronics",
    summary:
      "Placeholder for a hands-on build combining mechanical assembly, troubleshooting, and iterative testing.",
    image: roboticsImg,
    tags: ["ROBOTICS", "HANDS-ON", "PROBLEM SOLVING"],
    software: ["To be added"],
    skills: [
      "Mechanical Assembly",
      "Troubleshooting",
      "Testing & Iteration",
      "Team Collaboration",
    ],
    status: "Placeholder — details to be added",
    overview:
      "Project details coming soon. This case study will document a physical build, the mechanical decisions behind it, and how it was tested.",
    objective: "To be added: what the build needed to accomplish.",
    role: "To be added: Briara's role on the build team.",
    process: [
      { step: "Planning", detail: "To be added." },
      { step: "Fabrication & Assembly", detail: "To be added." },
      { step: "Testing", detail: "To be added." },
      { step: "Refinement", detail: "To be added." },
    ],
    result: "Final build performance to be added.",
    lessons: ["Lessons learned to be added once the project is documented."],
    gallery: [
      { caption: "Build photo — placeholder" },
      { caption: "Testing setup — placeholder" },
    ],
  },
];

export const getProject = (slug: string) =>
  projects.find((p) => p.slug === slug);


===== src/hooks/use-mobile.tsx =====
import * as React from "react";

const MOBILE_BREAKPOINT = 768;

export function useIsMobile() {
  const [isMobile, setIsMobile] = React.useState<boolean | undefined>(undefined);

  React.useEffect(() => {
    const mql = window.matchMedia(`(max-width: ${MOBILE_BREAKPOINT - 1}px)`);
    const onChange = () => {
      setIsMobile(window.innerWidth < MOBILE_BREAKPOINT);
    };
    mql.addEventListener("change", onChange);
    setIsMobile(window.innerWidth < MOBILE_BREAKPOINT);
    return () => mql.removeEventListener("change", onChange);
  }, []);

  return !!isMobile;
}


===== src/lib/error-capture.ts =====
// Captures the original Error out-of-band so server.ts can recover the stack
// when h3 has already swallowed the throw into a generic 500 Response.

let lastCapturedError: { error: unknown; at: number } | undefined;
const TTL_MS = 5_000;

function record(error: unknown) {
  lastCapturedError = { error, at: Date.now() };
}

// h3's HTTPError serializes to {"status":500,"unhandled":true,"message":"HTTPError"} —
// no stack, no cause — so a plain console.error(error) reaches the log pipeline with
// the failure detail stripped. Expand Error-like args into a string that keeps the
// message, stack, and the full cause chain.
const CAUSE_DEPTH_LIMIT = 5;
const DESCRIPTION_LENGTH_LIMIT = 8_000;

export function describeError(error: unknown): string {
  const parts: string[] = [];
  let current: unknown = error;
  for (let depth = 0; depth < CAUSE_DEPTH_LIMIT && current != null; depth++) {
    if (!(current instanceof Error)) {
      parts.push(typeof current === "string" ? current : safeStringify(current));
      break;
    }
    const label = depth === 0 ? "" : "caused by: ";
    const status = describeStatus(current);
    parts.push(`${label}${current.stack ?? `${current.name}: ${current.message}`}${status}`);
    current = current.cause;
  }
  return parts.join("\n").slice(0, DESCRIPTION_LENGTH_LIMIT);
}

function describeStatus(error: Error): string {
  const { status, statusCode } = error as { status?: unknown; statusCode?: unknown };
  const value = status ?? statusCode;
  return typeof value === "number" ? ` (status ${value})` : "";
}

function safeStringify(value: unknown): string {
  try {
    return JSON.stringify(value) ?? String(value);
  } catch {
    return String(value);
  }
}

function isErrorLike(value: unknown): value is Error {
  return value instanceof Error;
}

// Wrap console.error so errors logged by any layer — including h3's internal
// unhandled-error logging, which this file cannot hook directly — are both
// recorded for consumeLastCapturedError and expanded before serialization.
const originalConsoleError = console.error.bind(console);
console.error = (...args: unknown[]) => {
  const expanded = args.map((arg) => {
    if (!isErrorLike(arg)) return arg;
    record(arg);
    return describeError(arg);
  });
  originalConsoleError(...expanded);
};

if (typeof globalThis.addEventListener === "function") {
  globalThis.addEventListener("error", (event) => record((event as ErrorEvent).error ?? event));
  globalThis.addEventListener("unhandledrejection", (event) =>
    record((event as PromiseRejectionEvent).reason),
  );
}

export function consumeLastCapturedError(): unknown {
  if (!lastCapturedError) return undefined;
  if (Date.now() - lastCapturedError.at > TTL_MS) {
    lastCapturedError = undefined;
    return undefined;
  }
  const { error } = lastCapturedError;
  lastCapturedError = undefined;
  return error;
}


===== src/lib/error-page.ts =====
export function renderErrorPage(): string {
  return `<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>This page didn't load</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      body { font: 15px/1.5 system-ui, -apple-system, sans-serif; background: #fafafa; color: #111; display: grid; place-items: center; min-height: 100vh; margin: 0; padding: 1.5rem; }
      .card { max-width: 28rem; width: 100%; text-align: center; padding: 2rem; }
      h1 { font-size: 1.25rem; margin: 0 0 0.5rem; }
      p { color: #4b5563; margin: 0 0 1.5rem; }
      .actions { display: flex; gap: 0.5rem; justify-content: center; flex-wrap: wrap; }
      a, button { padding: 0.5rem 1rem; border-radius: 0.375rem; font: inherit; cursor: pointer; text-decoration: none; border: 1px solid transparent; }
      .primary { background: #111; color: #fff; }
      .secondary { background: #fff; color: #111; border-color: #d1d5db; }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>This page didn't load</h1>
      <p>Something went wrong on our end. You can try refreshing or head back home.</p>
      <div class="actions">
        <button class="primary" onclick="location.reload()">Try again</button>
        <a class="secondary" href="/">Go home</a>
      </div>
    </div>
  </body>
</html>`;
}


===== src/lib/lovable-error-reporting.ts =====
type LovableErrorOptions = {
  mechanism?: "manual" | "onerror" | "unhandledrejection" | "react_error_boundary";
  handled?: boolean;
  severity?: "error" | "warning" | "info";
};

type LovableEvents = {
  captureException?: (
    error: unknown,
    context?: Record<string, unknown>,
    options?: LovableErrorOptions,
  ) => void;
};

declare global {
  interface Window {
    __lovableEvents?: LovableEvents;
    __lovableReportRuntimeError?: (payload: {
      message: string;
      stack?: string;
      filename?: string;
    }) => void;
  }
}

export function reportLovableError(error: unknown, context: Record<string, unknown> = {}) {
  if (typeof window === "undefined") return;
  window.__lovableEvents?.captureException?.(
    error,
    {
      source: "react_error_boundary",
      route: window.location.pathname,
      ...context,
    },
    {
      mechanism: "react_error_boundary",
      handled: false,
      severity: "error",
    },
  );
  // Prod React does not rethrow boundary-caught errors to window.onerror, so the
  // editor's telemetry never sees them. Forward to lovable.js's reporting hook,
  // which is present only inside the editor preview.
  // Loaders and server fns commonly throw a raw Response; String(it) is the
  // opaque "[object Response]", so pull out the status and URL instead.
  const message =
    error instanceof Response
      ? `Response ${error.status}${error.url ? ` at ${error.url}` : ""}`
      : error instanceof Error
        ? error.message
        : String(error);
  const stack = error instanceof Error ? error.stack : undefined;
  window.__lovableReportRuntimeError?.({
    message,
    ...(stack !== undefined && { stack }),
    filename: window.location.pathname,
  });
}


===== src/lib/utils.ts =====
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}


===== src/router.tsx =====
import { QueryClient } from "@tanstack/react-query";
import { createRouter } from "@tanstack/react-router";
import { routeTree } from "./routeTree.gen";

export const getRouter = () => {
  const queryClient = new QueryClient();

  const router = createRouter({
    routeTree,
    context: { queryClient },
    scrollRestoration: true,
    defaultPreloadStaleTime: 0,
  });

  return router;
};


===== src/routes/__root.tsx =====
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import {
  Outlet,
  Link,
  createRootRouteWithContext,
  useRouter,
  HeadContent,
  Scripts,
} from "@tanstack/react-router";
import { useEffect, type ReactNode } from "react";

import appCss from "../styles.css?url";
import { reportLovableError } from "../lib/lovable-error-reporting";

function NotFoundComponent() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background px-4">
      <div className="max-w-md text-center">
        <h1 className="text-7xl font-bold text-foreground">404</h1>
        <h2 className="mt-4 text-xl font-semibold text-foreground">Page not found</h2>
        <p className="mt-2 text-sm text-muted-foreground">
          The page you're looking for doesn't exist or has been moved.
        </p>
        <div className="mt-6">
          <Link
            to="/"
            className="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90"
          >
            Go home
          </Link>
        </div>
      </div>
    </div>
  );
}

function ErrorComponent({ error, reset }: { error: Error; reset: () => void }) {
  console.error(error);
  const router = useRouter();
  useEffect(() => {
    reportLovableError(error, { boundary: "tanstack_root_error_component" });
  }, [error]);

  return (
    <div className="flex min-h-screen items-center justify-center bg-background px-4">
      <div className="max-w-md text-center">
        <h1 className="text-xl font-semibold tracking-tight text-foreground">
          This page didn't load
        </h1>
        <p className="mt-2 text-sm text-muted-foreground">
          Something went wrong on our end. You can try refreshing or head back home.
        </p>
        <div className="mt-6 flex flex-wrap justify-center gap-2">
          <button
            onClick={() => {
              router.invalidate();
              reset();
            }}
            className="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90"
          >
            Try again
          </button>
          <a
            href="/"
            className="inline-flex items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent"
          >
            Go home
          </a>
        </div>
      </div>
    </div>
  );
}

export const Route = createRootRouteWithContext<{ queryClient: QueryClient }>()({
  head: () => ({
    meta: [
      { charSet: "utf-8" },
      { name: "viewport", content: "width=device-width, initial-scale=1" },
      { title: "Lovable App" },
      { name: "description", content: "Lovable Generated Project" },
      { name: "author", content: "Lovable" },
      { property: "og:title", content: "Lovable App" },
      { property: "og:description", content: "Lovable Generated Project" },
      { property: "og:type", content: "website" },
      { name: "twitter:card", content: "summary_large_image" },
      { name: "twitter:site", content: "@Lovable" },
    ],
    links: [
      {
        rel: "stylesheet",
        href: appCss,
      },
      { rel: "icon", href: "/favicon.ico", type: "image/x-icon" },
    ],
  }),
  shellComponent: RootShell,
  component: RootComponent,
  notFoundComponent: NotFoundComponent,
  errorComponent: ErrorComponent,
});

function RootShell({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <head>
        <HeadContent />
      </head>
      <body>
        {children}
        <Scripts />
      </body>
    </html>
  );
}

function RootComponent() {
  const { queryClient } = Route.useRouteContext();

  return (
    <QueryClientProvider client={queryClient}>
      {/* Required: nested routes render here. Removing <Outlet /> breaks all child routes. */}
      <Outlet />
    </QueryClientProvider>
  );
}


===== src/routes/about.tsx =====
import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";

import { PageHeader } from "@/components/page-header";

export const Route = createFileRoute("/about")({
  head: () => ({
    meta: [
      { title: "About — Briara Broussard, Engineering Student" },
      {
        name: "description",
        content:
          "Briara Broussard is a Mechanical Engineering Technology student at the University of Houston focused on mechanical design, manufacturing, CAD, and hands-on problem solving.",
      },
      { property: "og:title", content: "About Briara Broussard" },
      {
        property: "og:description",
        content:
          "Mechanical Engineering Technology student at the University of Houston focused on design, manufacturing, and CAD.",
      },
    ],
  }),
  component: About,
});

const interests = [
  ["Mechanical Design", "Turning requirements into parts that can actually be made."],
  ["Manufacturing", "Understanding how a design behaves on the shop floor."],
  ["CAD", "Building clean, editable models with clear design intent."],
  ["Hands-On Engineering", "Learning by building, measuring, and iterating."],
  ["Problem Solving", "Breaking complex problems into testable steps."],
  ["Technical Growth", "Constantly adding new software and shop skills."],
];

const stats = [
  ["01 — Student", "University of Houston"],
  ["02 — Focus", "Design • Manufacturing"],
  ["03 — Tools", "SolidWorks • AutoCAD"],
  ["04 — Goal", "Engineering Internship"],
];

function About() {
  return (
    <>
      <PageHeader eyebrow="About Me" title="Driven. Focused. Ready to Engineer." />

      <section className="mx-auto grid max-w-7xl gap-14 px-5 py-16 sm:px-8 lg:grid-cols-[1.4fr_1fr]">
        <div className="space-y-5 text-base leading-relaxed text-muted-foreground">
          <p>
            I'm Briara Broussard, a Mechanical Engineering Technology student at the University
            of Houston. My program sits right where design meets the shop floor — it's as much
            about how something gets built as how it gets drawn, and that's exactly the kind of
            engineering I want to do.
          </p>
          <p>
            Most of my time goes into learning the tools of the trade: modeling parts and
            assemblies in SolidWorks, producing dimensioned technical drawings in AutoCAD, and
            working through structured coursework and certification training to sharpen those
            skills. I care about clean design intent, drawings someone else can actually read,
            and solutions that hold up when they leave the screen.
          </p>
          <p>
            Outside of coursework, I'm the person who wants to take the thing apart and
            understand why it works. I'm organized, dependable, and comfortable in
            fast-moving environments — habits built through work and team settings that
            translate directly into engineering teams.
          </p>
          <p className="text-foreground">
            Right now I'm looking for an engineering internship where I can contribute to real
            design and manufacturing work and keep building technical depth.
          </p>

          <div className="flex flex-wrap gap-3 pt-4">
            <Link
              to="/projects"
              className="tech-label group inline-flex items-center gap-2 bg-primary px-6 py-3.5 text-primary-foreground transition-colors hover:bg-primary-bright"
            >
              See My Projects
              <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" />
            </Link>
            <Link
              to="/contact"
              className="tech-label inline-flex items-center border border-border px-6 py-3.5 transition-colors hover:border-primary hover:text-primary"
            >
              Get In Touch
            </Link>
          </div>
        </div>

        <div className="relative">
          <div className="pointer-events-none absolute -inset-4 blueprint-grid opacity-30" />
          <div className="relative divide-y divide-border border border-border bg-surface">
            {stats.map(([k, v]) => (
              <div key={k} className="px-6 py-6">
                <p className="tech-label text-primary">{k}</p>
                <p className="mt-2 font-display text-lg font-semibold">{v}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="border-t border-border bg-surface">
        <div className="mx-auto max-w-7xl px-5 py-16 sm:px-8">
          <p className="tech-label text-primary">What I'm Into</p>
          <h2 className="mt-3 text-3xl font-bold sm:text-4xl">Areas of Interest</h2>
          <div className="mt-10 grid gap-px border border-border bg-border sm:grid-cols-2 lg:grid-cols-3">
            {interests.map(([title, detail]) => (
              <div
                key={title}
                className="group bg-surface-alt p-6 transition-colors hover:bg-background"
              >
                <div className="h-px w-8 bg-primary transition-all group-hover:w-14" />
                <h3 className="mt-4 font-display text-lg font-semibold">{title}</h3>
                <p className="mt-2 text-sm leading-relaxed text-muted-foreground">{detail}</p>
              </div>
            ))}
          </div>
        </div>
      </section>
    </>
  );
}


===== src/routes/experience.tsx =====
import { createFileRoute } from "@tanstack/react-router";

import { PageHeader } from "@/components/page-header";

export const Route = createFileRoute("/experience")({
  head: () => ({
    meta: [
      { title: "Experience — Briara Broussard" },
      {
        name: "description",
        content:
          "Work experience of Briara Broussard, including the University of Houston Recreation Center, Charley's Cheesesteaks, Hibbett Sports, and childcare assistance.",
      },
      { property: "og:title", content: "Experience — Briara Broussard" },
      {
        property: "og:description",
        content: "A timeline of work experience and the transferable skills built along the way.",
      },
    ],
  }),
  component: Experience,
});

const roles = [
  {
    org: "University of Houston Recreation Center",
    title: "ID Check Attendant / Administrative Assistant",
    period: "09/2025 — Present",
    description:
      "Verify credentials, provide customer service, enforce facility policies, organize records, and support daily administrative operations.",
    skills: ["Attention to Detail", "Record Keeping", "Policy Compliance"],
  },
  {
    org: "Charley's Cheesesteaks",
    title: "Cashier / Crew Member",
    period: "Summer 2026",
    description:
      "Processed orders and payments, provided customer service, maintained an organized workspace, and collaborated in a fast-paced environment.",
    skills: ["Time Management", "Team Collaboration", "Process Consistency"],
  },
  {
    org: "Hibbett Sports",
    title: "Sales Associate",
    period: "05/2024 — 06/2025",
    description:
      "Assisted customers, maintained store organization and displays, managed inventory, and supported daily operations.",
    skills: ["Inventory Management", "Organization", "Communication"],
  },
  {
    org: "Family Daycare",
    title: "Childcare Assistant",
    period: "2017 — Present",
    description:
      "Assisted with supervision and activities while maintaining a safe environment and communicating with children and parents.",
    skills: ["Responsibility", "Safety Awareness", "Communication"],
  },
];

function Experience() {
  return (
    <>
      <PageHeader
        eyebrow="Experience"
        title="Where I've Worked"
        intro="Roles that built the reliability, organization, and communication habits I carry into engineering work."
      />

      <section className="mx-auto max-w-4xl px-5 py-16 sm:px-8">
        <ol className="relative border-l border-border pl-8 sm:pl-12">
          {roles.map((r) => (
            <li key={r.org} className="relative pb-12 last:pb-0">
              <span className="absolute -left-[calc(2rem+5px)] top-1.5 h-2.5 w-2.5 rotate-45 border border-primary bg-background sm:-left-[calc(3rem+5px)]" />
              <div className="border border-border bg-surface p-6 transition-colors hover:border-primary/60 sm:p-7">
                <p className="tech-label text-primary">{r.period}</p>
                <h2 className="mt-3 font-display text-xl font-bold sm:text-2xl">{r.org}</h2>
                <p className="mt-1 text-sm font-medium text-foreground">{r.title}</p>
                <p className="mt-4 text-sm leading-relaxed text-muted-foreground">
                  {r.description}
                </p>
                <div className="mt-5 flex flex-wrap gap-2 border-t border-border pt-5">
                  {r.skills.map((s) => (
                    <span
                      key={s}
                      className="tech-label border border-border px-2.5 py-1 text-muted-foreground"
                    >
                      {s}
                    </span>
                  ))}
                </div>
              </div>
            </li>
          ))}
        </ol>
      </section>
    </>
  );
}


===== src/routes/index.tsx =====
import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Github, Linkedin, Mail } from "lucide-react";

import heroCad from "@/assets/hero-cad.jpg";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "Briara Broussard — Mechanical Engineering Technology Student" },
      {
        name: "description",
        content:
          "Portfolio of Briara Broussard, a Mechanical Engineering Technology student at the University of Houston seeking engineering internships in design and manufacturing.",
      },
      { property: "og:title", content: "Briara Broussard — Engineering Portfolio" },
      {
        property: "og:description",
        content:
          "Mechanical Engineering Technology student at the University of Houston. CAD, technical drawing, and hands-on engineering projects.",
      },
    ],
  }),
  component: Home,
});

function Home() {
  return (
    <>
      <section className="relative overflow-hidden">
        <div className="pointer-events-none absolute inset-0 blueprint-grid opacity-50" />
        <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_70%_35%,color-mix(in_oklab,var(--color-primary)_14%,transparent),transparent_60%)]" />

        <div className="relative mx-auto grid max-w-7xl items-center gap-14 px-5 py-16 sm:px-8 sm:py-24 lg:grid-cols-2 lg:gap-16 lg:py-28">
          <div>
            <p className="tech-label reveal text-primary">
              Mechanical Engineering Technology Student
            </p>

            <h1 className="reveal mt-5 text-5xl font-bold leading-[0.95] sm:text-6xl lg:text-7xl xl:text-8xl">
              BRIARA
              <br />
              BROUSSARD
            </h1>

            <div className="reveal mt-7 h-[3px] w-24 bg-primary" />

            <p className="reveal mt-7 max-w-xl text-base leading-relaxed text-muted-foreground sm:text-lg">
              Mechanical Engineering Technology student at the University of Houston with a
              passion for design, problem-solving, and creating practical solutions.
            </p>

            <div className="reveal mt-9 flex flex-wrap items-center gap-3">
              <Link
                to="/projects"
                className="tech-label group inline-flex items-center gap-2 bg-primary px-6 py-3.5 text-primary-foreground transition-colors hover:bg-primary-bright"
              >
                View My Work
                <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" />
              </Link>
              <Link
                to="/resume"
                className="tech-label inline-flex items-center gap-2 border border-border px-6 py-3.5 text-foreground transition-colors hover:border-primary hover:text-primary"
              >
                View Resume
              </Link>
            </div>

            <div className="reveal mt-10 flex items-center gap-3">
              <span className="tech-label text-muted-foreground">Connect</span>
              <span className="h-px w-8 bg-border" />
              <IconLink href="https://www.linkedin.com" label="LinkedIn">
                <Linkedin className="h-4 w-4" />
              </IconLink>
              <IconLink href="https://github.com" label="GitHub">
                <Github className="h-4 w-4" />
              </IconLink>
              <IconLink href="mailto:Broussardbriara@gmail.com" label="Email">
                <Mail className="h-4 w-4" />
              </IconLink>
            </div>
          </div>

          <div className="reveal relative">
            <div className="corner-ticks relative border border-border bg-surface">
              <div className="flex items-center justify-between border-b border-border px-4 py-2.5">
                <span className="tech-label text-muted-foreground">DWG — ASSEMBLY VIEW</span>
                <span className="tech-label text-primary">REV A</span>
              </div>
              <div className="relative">
                <div className="pointer-events-none absolute inset-0 z-10 blueprint-grid-fine opacity-25" />
                <img
                  src={heroCad}
                  alt="Technical isometric render of a gear assembly with blueprint dimension lines"
                  width={1200}
                  height={1200}
                  className="aspect-square w-full object-cover"
                />
              </div>
              <div className="grid grid-cols-3 divide-x divide-border border-t border-border">
                {[
                  ["SCALE", "1:1"],
                  ["UNITS", "MM"],
                  ["SHEET", "01 / 01"],
                ].map(([k, v]) => (
                  <div key={k} className="px-4 py-3">
                    <p className="tech-label text-muted-foreground">{k}</p>
                    <p className="mt-1 font-display text-sm font-semibold">{v}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>

      <section className="border-y border-border bg-surface">
        <div className="mx-auto grid max-w-7xl grid-cols-2 divide-border px-5 sm:px-8 lg:grid-cols-4 lg:divide-x">
          {[
            ["01 — STUDENT", "University of Houston"],
            ["02 — FOCUS", "Design • Manufacturing"],
            ["03 — TOOLS", "SolidWorks • AutoCAD"],
            ["04 — GOAL", "Engineering Internship"],
          ].map(([k, v]) => (
            <div key={k} className="px-2 py-8 lg:px-6">
              <p className="tech-label text-primary">{k}</p>
              <p className="mt-2 font-display text-base font-semibold sm:text-lg">{v}</p>
            </div>
          ))}
        </div>
      </section>

      <section className="mx-auto max-w-7xl px-5 py-20 sm:px-8">
        <div className="flex flex-wrap items-end justify-between gap-6">
          <div>
            <p className="tech-label text-primary">Featured Projects</p>
            <h2 className="mt-3 text-3xl font-bold sm:text-4xl">Things I've Built</h2>
          </div>
          <Link
            to="/projects"
            className="tech-label group inline-flex items-center gap-2 text-muted-foreground hover:text-primary"
          >
            All Projects
            <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" />
          </Link>
        </div>

        <div className="mt-10 grid gap-5 md:grid-cols-3">
          {[
            ["01", "SolidWorks Project", "CAD / Part Design", "/projects/solidworks-project"],
            ["02", "Technical Drawing", "Engineering Graphics", "/projects/technical-drawing"],
            ["03", "Robotics / Hands-On", "Build / Mechatronics", "/projects/robotics-hands-on"],
          ].map(([n, title, cat]) => (
            <div
              key={n}
              className="border border-border bg-surface-alt p-6 transition-colors hover:border-primary/60"
            >
              <p className="tech-label text-primary">{n}</p>
              <h3 className="mt-4 text-xl font-bold">{title}</h3>
              <p className="mt-2 text-sm text-muted-foreground">{cat}</p>
            </div>
          ))}
        </div>
      </section>
    </>
  );
}

function IconLink({
  href,
  label,
  children,
}: {
  href: string;
  label: string;
  children: React.ReactNode;
}) {
  return (
    <a
      href={href}
      target="_blank"
      rel="noreferrer noopener"
      aria-label={label}
      className="grid h-10 w-10 place-items-center border border-border text-muted-foreground transition-colors hover:border-primary hover:text-primary"
    >
      {children}
    </a>
  );
}


===== src/routes/projects.$slug.tsx =====
import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ArrowLeft, ArrowRight } from "lucide-react";

import { getProject, projects } from "@/data/projects";

export const Route = createFileRoute("/projects/$slug")({
  loader: ({ params }) => {
    const project = getProject(params.slug);
    if (!project) throw notFound();
    return { project };
  },
  head: ({ loaderData }) => {
    if (!loaderData) {
      return {
        meta: [{ title: "Project not found — Briara Broussard" }, { name: "robots", content: "noindex" }],
      };
    }
    const { project } = loaderData;
    return {
      meta: [
        { title: `${project.title} — Briara Broussard` },
        { name: "description", content: project.summary },
        { property: "og:title", content: `${project.title} — Engineering Case Study` },
        { property: "og:description", content: project.summary },
      ],
    };
  },
  notFoundComponent: ProjectNotFound,
  component: ProjectDetail,
});

function ProjectNotFound() {
  return (
    <div className="mx-auto max-w-7xl px-5 py-24 sm:px-8">
      <h1 className="text-3xl font-bold">Project not found</h1>
      <p className="mt-3 text-muted-foreground">This case study doesn't exist yet.</p>
      <Link to="/projects" className="tech-label mt-6 inline-flex text-primary">
        Back to projects
      </Link>
    </div>
  );
}

function ProjectDetail() {
  const { project } = Route.useLoaderData();
  const others = projects.filter((p) => p.slug !== project.slug);

  return (
    <>
      <section className="relative overflow-hidden border-b border-border">
        <div className="pointer-events-none absolute inset-0 blueprint-grid opacity-35" />
        <div className="relative mx-auto max-w-7xl px-5 py-14 sm:px-8 sm:py-20">
          <Link
            to="/projects"
            className="tech-label inline-flex items-center gap-2 text-muted-foreground hover:text-primary"
          >
            <ArrowLeft className="h-4 w-4" />
            All Projects
          </Link>
          <p className="tech-label mt-8 text-primary">
            Project {project.index} — {project.category}
          </p>
          <h1 className="mt-4 max-w-3xl text-4xl font-bold leading-[1.05] sm:text-5xl lg:text-6xl">
            {project.title}
          </h1>
          <div className="mt-6 h-px w-20 bg-primary" />
          <p className="mt-6 max-w-2xl leading-relaxed text-muted-foreground">{project.summary}</p>
          <span className="tech-label mt-6 inline-block border border-primary/50 bg-primary/10 px-3 py-1.5 text-primary">
            {project.status}
          </span>
        </div>
      </section>

      <section className="mx-auto max-w-7xl px-5 pt-12 sm:px-8">
        <div className="corner-ticks relative border border-border">
          <div className="pointer-events-none absolute inset-0 z-10 blueprint-grid-fine opacity-20" />
          <img
            src={project.image}
            alt={`${project.title} main visual`}
            loading="lazy"
            width={1200}
            height={800}
            className="aspect-[3/2] w-full object-cover"
          />
        </div>
      </section>

      <section className="mx-auto grid max-w-7xl gap-12 px-5 py-16 sm:px-8 lg:grid-cols-[1.5fr_1fr]">
        <div className="space-y-12">
          <Block title="Overview">{project.overview}</Block>
          <Block title="Objective">{project.objective}</Block>
          <Block title="My Role">{project.role}</Block>

          <div>
            <SectionTitle>Design Process</SectionTitle>
            <ol className="mt-6 space-y-px border border-border bg-border">
              {project.process.map((s, i) => (
                <li key={s.step} className="flex gap-5 bg-surface p-5">
                  <span className="tech-label shrink-0 text-primary">
                    {String(i + 1).padStart(2, "0")}
                  </span>
                  <div>
                    <p className="font-display font-semibold">{s.step}</p>
                    <p className="mt-1 text-sm text-muted-foreground">{s.detail}</p>
                  </div>
                </li>
              ))}
            </ol>
          </div>

          <div>
            <SectionTitle>Images & CAD Screenshots</SectionTitle>
            <div className="mt-6 grid gap-4 sm:grid-cols-2">
              {project.gallery.map((g) => (
                <div
                  key={g.caption}
                  className="flex aspect-[4/3] flex-col items-center justify-center border border-dashed border-border bg-surface blueprint-grid-fine"
                >
                  <span className="tech-label text-muted-foreground">{g.caption}</span>
                </div>
              ))}
            </div>
          </div>

          <Block title="Final Result">{project.result}</Block>

          <div>
            <SectionTitle>Lessons Learned</SectionTitle>
            <ul className="mt-6 space-y-3">
              {project.lessons.map((l) => (
                <li key={l} className="flex gap-3 text-muted-foreground">
                  <span className="mt-2.5 h-px w-4 shrink-0 bg-primary" />
                  <span className="leading-relaxed">{l}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <aside className="h-fit space-y-px border border-border bg-border lg:sticky lg:top-24">
          <SidePanel title="Software Used" items={project.software} />
          <SidePanel title="Skills Used" items={project.skills} />
          <div className="bg-surface p-6">
            <p className="tech-label text-muted-foreground">Tags</p>
            <div className="mt-3 flex flex-wrap gap-2">
              {project.tags.map((t) => (
                <span key={t} className="tech-label border border-border px-2.5 py-1 text-primary">
                  {t}
                </span>
              ))}
            </div>
          </div>
        </aside>
      </section>

      <section className="border-t border-border bg-surface">
        <div className="mx-auto max-w-7xl px-5 py-14 sm:px-8">
          <p className="tech-label text-primary">Next</p>
          <div className="mt-6 grid gap-4 sm:grid-cols-2">
            {others.map((p) => (
              <Link
                key={p.slug}
                to="/projects/$slug"
                params={{ slug: p.slug }}
                className="group flex items-center justify-between gap-4 border border-border bg-surface-alt p-6 transition-colors hover:border-primary/60"
              >
                <div>
                  <p className="tech-label text-muted-foreground">{p.index}</p>
                  <p className="mt-2 font-display text-lg font-semibold">{p.title}</p>
                </div>
                <ArrowRight className="h-5 w-5 text-primary transition-transform group-hover:translate-x-1" />
              </Link>
            ))}
          </div>
        </div>
      </section>
    </>
  );
}

function SectionTitle({ children }: { children: React.ReactNode }) {
  return (
    <div>
      <p className="tech-label text-primary">{children}</p>
      <div className="mt-3 h-px w-full bg-border" />
    </div>
  );
}

function Block({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <div>
      <SectionTitle>{title}</SectionTitle>
      <p className="mt-5 leading-relaxed text-muted-foreground">{children}</p>
    </div>
  );
}

function SidePanel({ title, items }: { title: string; items: string[] }) {
  return (
    <div className="bg-surface p-6">
      <p className="tech-label text-muted-foreground">{title}</p>
      <ul className="mt-3 space-y-2">
        {items.map((i) => (
          <li key={i} className="text-sm">
            {i}
          </li>
        ))}
      </ul>
    </div>
  );
}


===== src/routes/projects.index.tsx =====
import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";

import { PageHeader } from "@/components/page-header";
import { projects } from "@/data/projects";

export const Route = createFileRoute("/projects/")({
  head: () => ({
    meta: [
      { title: "Projects — Briara Broussard Engineering Portfolio" },
      {
        name: "description",
        content:
          "CAD, technical drawing, and hands-on engineering projects by Briara Broussard, Mechanical Engineering Technology student at the University of Houston.",
      },
      { property: "og:title", content: "Engineering Projects — Briara Broussard" },
      {
        property: "og:description",
        content:
          "SolidWorks modeling, AutoCAD technical drawings, and hands-on builds presented as engineering case studies.",
      },
    ],
  }),
  component: ProjectsPage,
});

function ProjectsPage() {
  return (
    <>
      <PageHeader
        eyebrow="Featured Projects"
        title="Things I've Built"
        intro="A growing set of engineering case studies covering CAD modeling, technical drawing, and hands-on builds. Each entry documents the objective, process, tools, and outcome."
      />

      <section className="mx-auto max-w-7xl space-y-6 px-5 py-16 sm:px-8">
        {projects.map((p, i) => (
          <article
            key={p.slug}
            className="group grid overflow-hidden border border-border bg-surface transition-colors hover:border-primary/60 lg:grid-cols-2"
          >
            <div className={`relative ${i % 2 === 1 ? "lg:order-2" : ""}`}>
              <div className="pointer-events-none absolute inset-0 z-10 blueprint-grid-fine opacity-20" />
              <img
                src={p.image}
                alt={`${p.title} preview`}
                loading="lazy"
                width={1200}
                height={800}
                className="h-full min-h-64 w-full object-cover transition-transform duration-500 group-hover:scale-[1.03]"
              />
              <span className="tech-label absolute left-0 top-0 z-20 bg-primary px-3 py-1.5 text-primary-foreground">
                {p.index}
              </span>
            </div>

            <div className="flex flex-col justify-center gap-5 p-7 sm:p-10">
              <div>
                <p className="tech-label text-primary">{p.category}</p>
                <h2 className="mt-3 text-2xl font-bold sm:text-3xl">{p.title}</h2>
                <p className="mt-3 text-sm leading-relaxed text-muted-foreground">{p.summary}</p>
              </div>

              <div className="flex flex-wrap gap-2">
                {p.tags.map((t) => (
                  <span
                    key={t}
                    className="tech-label border border-border px-2.5 py-1 text-muted-foreground"
                  >
                    {t}
                  </span>
                ))}
              </div>

              <dl className="grid gap-4 border-t border-border pt-5 sm:grid-cols-2">
                <div>
                  <dt className="tech-label text-muted-foreground">Software</dt>
                  <dd className="mt-1.5 text-sm">{p.software.join(" · ")}</dd>
                </div>
                <div>
                  <dt className="tech-label text-muted-foreground">Skills</dt>
                  <dd className="mt-1.5 text-sm">{p.skills.slice(0, 3).join(" · ")}</dd>
                </div>
              </dl>

              <Link
                to="/projects/$slug"
                params={{ slug: p.slug }}
                className="tech-label inline-flex w-fit items-center gap-2 border border-border px-5 py-3 transition-colors hover:border-primary hover:text-primary"
              >
                View Project
                <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" />
              </Link>
            </div>
          </article>
        ))}
      </section>
    </>
  );
}


===== src/routes/skills.tsx =====
import { createFileRoute } from "@tanstack/react-router";
import { Award } from "lucide-react";

import { PageHeader } from "@/components/page-header";

export const Route = createFileRoute("/skills")({
  head: () => ({
    meta: [
      { title: "Technical Skills — Briara Broussard" },
      {
        name: "description",
        content:
          "SolidWorks, AutoCAD, SolidProfessor, and Microsoft Office skills plus engineering and professional strengths and certifications held by Briara Broussard.",
      },
      { property: "og:title", content: "Technical Skills — Briara Broussard" },
      {
        property: "og:description",
        content:
          "CAD software proficiency, engineering skills, and 20+ technical certifications.",
      },
    ],
  }),
  component: Skills,
});

const software = [
  { name: "SolidWorks", level: "Proficient", bars: 4 },
  { name: "AutoCAD", level: "Proficient", bars: 4 },
  { name: "SolidProfessor", level: "Working knowledge", bars: 3 },
  { name: "Microsoft Office", level: "Advanced", bars: 5 },
];

const skills = [
  "3D Modeling",
  "Technical Drawing",
  "Problem Solving",
  "Team Collaboration",
  "Time Management",
  "Organization",
  "Technical Communication",
  "Design",
  "Manufacturing",
];

const certs = [
  {
    title: "20+ Technical Certifications",
    detail: "AutoCAD, SolidProfessor, Everfi, MSI, and TEEX coursework and training programs.",
  },
  {
    title: "American Red Cross",
    detail: "CPR / AED / First Aid certified.",
  },
];

function Skills() {
  return (
    <>
      <PageHeader
        eyebrow="Technical Skills"
        title="Tools I Use"
        intro="The software, engineering fundamentals, and professional habits I bring to a design or manufacturing team."
      />

      <section className="mx-auto grid max-w-7xl gap-14 px-5 py-16 sm:px-8 lg:grid-cols-2">
        <div>
          <p className="tech-label text-primary">Software</p>
          <div className="mt-6 space-y-px border border-border bg-border">
            {software.map((s) => (
              <div key={s.name} className="bg-surface p-6">
                <div className="flex items-baseline justify-between gap-4">
                  <h3 className="font-display text-lg font-semibold">{s.name}</h3>
                  <span className="tech-label text-muted-foreground">{s.level}</span>
                </div>
                <div className="mt-4 flex gap-1.5" aria-hidden="true">
                  {Array.from({ length: 5 }).map((_, i) => (
                    <span
                      key={i}
                      className={`h-1.5 flex-1 ${i < s.bars ? "bg-primary" : "bg-border"}`}
                    />
                  ))}
                </div>
              </div>
            ))}
          </div>
          <p className="mt-4 text-xs text-muted-foreground">
            Indicators reflect self-assessed comfort level, not formal scoring.
          </p>
        </div>

        <div>
          <p className="tech-label text-primary">Engineering & Professional Skills</p>
          <div className="mt-6 flex flex-wrap gap-2.5">
            {skills.map((s) => (
              <span
                key={s}
                className="tech-label border border-border bg-surface px-4 py-2.5 text-foreground transition-colors hover:border-primary hover:text-primary"
              >
                {s}
              </span>
            ))}
          </div>

          <div className="relative mt-12 border border-border bg-surface-alt p-7">
            <div className="pointer-events-none absolute inset-0 blueprint-grid-fine opacity-20" />
            <div className="relative">
              <div className="flex items-center gap-3">
                <Award className="h-5 w-5 text-primary" />
                <p className="tech-label text-primary">Certifications & Training</p>
              </div>
              <div className="mt-6 space-y-6">
                {certs.map((c) => (
                  <div key={c.title}>
                    <h3 className="font-display text-lg font-semibold">{c.title}</h3>
                    <p className="mt-1.5 text-sm leading-relaxed text-muted-foreground">
                      {c.detail}
                    </p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>
    </>
  );
}


===== src/server.ts =====
import "./lib/error-capture";

import { consumeLastCapturedError } from "./lib/error-capture";
import { renderErrorPage } from "./lib/error-page";

type ServerEntry = {
  fetch: (request: Request, env: unknown, ctx: unknown) => Promise<Response> | Response;
};

let serverEntryPromise: Promise<ServerEntry> | undefined;

async function getServerEntry(): Promise<ServerEntry> {
  if (!serverEntryPromise) {
    serverEntryPromise = import("@tanstack/react-start/server-entry").then(
      (m) => (m.default ?? m) as ServerEntry,
    );
  }
  return serverEntryPromise;
}

// h3 swallows in-handler throws into a normal 500 Response with body
// {"unhandled":true,"message":"HTTPError"} — try/catch alone never fires for those.
async function normalizeCatastrophicSsrResponse(response: Response): Promise<Response> {
  if (response.status < 500) return response;
  const contentType = response.headers.get("content-type") ?? "";
  if (!contentType.includes("application/json")) return response;

  const body = await response.clone().text();
  if (!isH3SwallowedErrorBody(body)) return response;

  console.error(consumeLastCapturedError() ?? new Error(`h3 swallowed SSR error: ${body}`));
  return new Response(renderErrorPage(), {
    status: 500,
    headers: { "content-type": "text/html; charset=utf-8" },
  });
}

function isH3SwallowedErrorBody(body: string): boolean {
  try {
    const payload = JSON.parse(body) as { unhandled?: unknown; message?: unknown };
    return payload.unhandled === true && payload.message === "HTTPError";
  } catch {
    return false;
  }
}

export default {
  async fetch(request: Request, env: unknown, ctx: unknown) {
    try {
      const handler = await getServerEntry();
      const response = await handler.fetch(request, env, ctx);
      return await normalizeCatastrophicSsrResponse(response);
    } catch (error) {
      console.error(error);
      return new Response(renderErrorPage(), {
        status: 500,
        headers: { "content-type": "text/html; charset=utf-8" },
      });
    }
  },
};


===== src/start.ts =====
import { createStart, createCsrfMiddleware, createMiddleware } from "@tanstack/react-start";

import { renderErrorPage } from "./lib/error-page";

const errorMiddleware = createMiddleware().server(async ({ next }) => {
  try {
    return await next();
  } catch (error) {
    if (error != null && typeof error === "object" && "statusCode" in error) {
      throw error;
    }
    console.error(error);
    return new Response(renderErrorPage(), {
      status: 500,
      headers: { "content-type": "text/html; charset=utf-8" },
    });
  }
});

// Start installs this automatically when src/start.ts is absent; defining the
// file opts out, so re-add it explicitly to keep server functions protected
// from cross-site requests.
const csrfMiddleware = createCsrfMiddleware({
  filter: (ctx) => ctx.handlerType === "serverFn",
});

export const startInstance = createStart(() => ({
  requestMiddleware: [errorMiddleware, csrfMiddleware],
}));


===== src/styles.css =====
@import "tailwindcss" source(none);
@source "../src";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

/*
 * Briara Broussard — engineering portfolio design system.
 * Dark industrial / blueprint aesthetic. All colors in oklch.
 */

@theme inline {
  --radius-sm: calc(var(--radius) - 2px);
  --radius-md: var(--radius);
  --radius-lg: calc(var(--radius) + 2px);
  --radius-xl: calc(var(--radius) + 6px);
  --radius-2xl: calc(var(--radius) + 10px);
  --radius-3xl: calc(var(--radius) + 14px);
  --radius-4xl: calc(var(--radius) + 18px);

  --font-display: "Space Grotesk", ui-sans-serif, system-ui, sans-serif;
  --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;

  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-surface: var(--surface);
  --color-surface-alt: var(--surface-alt);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary-bright: var(--primary-bright);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-blueprint: var(--blueprint);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-ring-offset-background: var(--background);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --color-sidebar: var(--sidebar);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
}

:root {
  --radius: 0.25rem;

  /* #080A0D */
  --background: oklch(0.152 0.008 250);
  /* #F5F5F5 */
  --foreground: oklch(0.965 0 0);
  /* #0D1014 */
  --surface: oklch(0.181 0.008 250);
  /* #12161B */
  --surface-alt: oklch(0.211 0.009 250);
  --card: oklch(0.181 0.008 250);
  --card-foreground: oklch(0.965 0 0);
  --popover: oklch(0.181 0.008 250);
  --popover-foreground: oklch(0.965 0 0);
  /* #E31B23 */
  --primary: oklch(0.556 0.222 27.5);
  --primary-foreground: oklch(0.985 0 0);
  /* #FF2B35 */
  --primary-bright: oklch(0.63 0.235 26.5);
  --secondary: oklch(0.211 0.009 250);
  --secondary-foreground: oklch(0.965 0 0);
  --muted: oklch(0.211 0.009 250);
  /* #A7ADB5 */
  --muted-foreground: oklch(0.735 0.011 255);
  --accent: oklch(0.243 0.011 250);
  --accent-foreground: oklch(0.965 0 0);
  --destructive: oklch(0.556 0.222 27.5);
  --destructive-foreground: oklch(0.985 0 0);
  /* #252A30 */
  --border: oklch(0.278 0.01 253);
  /* #1B2733 */
  --blueprint: oklch(0.256 0.021 250);
  --input: oklch(0.278 0.01 253);
  --ring: oklch(0.556 0.222 27.5);
  --chart-1: oklch(0.556 0.222 27.5);
  --chart-2: oklch(0.63 0.235 26.5);
  --chart-3: oklch(0.735 0.011 255);
  --chart-4: oklch(0.278 0.01 253);
  --chart-5: oklch(0.256 0.021 250);
  --sidebar: oklch(0.181 0.008 250);
  --sidebar-foreground: oklch(0.965 0 0);
  --sidebar-primary: oklch(0.556 0.222 27.5);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.243 0.011 250);
  --sidebar-accent-foreground: oklch(0.965 0 0);
  --sidebar-border: oklch(0.278 0.01 253);
  --sidebar-ring: oklch(0.556 0.222 27.5);
}

@layer base {
  * {
    border-color: var(--color-border);
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--color-background);
    color: var(--color-foreground);
    font-family: var(--font-sans);
    -webkit-font-smoothing: antialiased;
  }

  h1,
  h2,
  h3,
  h4 {
    font-family: var(--font-display);
    letter-spacing: -0.03em;
  }

  ::selection {
    background: var(--color-primary);
    color: var(--color-primary-foreground);
  }
}

/* Blueprint grid backdrop */
@utility blueprint-grid {
  background-image:
    linear-gradient(to right, var(--color-blueprint) 1px, transparent 1px),
    linear-gradient(to bottom, var(--color-blueprint) 1px, transparent 1px);
  background-size: 56px 56px;
}

@utility blueprint-grid-fine {
  background-image:
    linear-gradient(to right, var(--color-blueprint) 1px, transparent 1px),
    linear-gradient(to bottom, var(--color-blueprint) 1px, transparent 1px);
  background-size: 14px 14px;
}

/* Small uppercase technical label */
@utility tech-label {
  font-family: var(--font-sans);
  font-size: 0.6875rem;
  font-weight: 600;
  letter-spacing: 0.22em;
  text-transform: uppercase;
}

@utility corner-ticks {
  position: relative;
  &::before,
  &::after {
    content: "";
    position: absolute;
    width: 10px;
    height: 10px;
    border-color: var(--color-primary);
    opacity: 0.75;
  }
  &::before {
    top: -1px;
    left: -1px;
    border-top: 1px solid;
    border-left: 1px solid;
  }
  &::after {
    bottom: -1px;
    right: -1px;
    border-bottom: 1px solid;
    border-right: 1px solid;
  }
}

/* Entrance motion */
@keyframes rise-in {
  from {
    opacity: 0;
    transform: translateY(14px);
  }
  to {
    opacity: 1;
    transform: none;
  }
}

@utility reveal {
  opacity: 0;
  animation: rise-in 0.5s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

@media (prefers-reduced-motion: reduce) {
  .reveal {
    animation: none;
    opacity: 1;
  }
  html {
    scroll-behavior: auto;
  }
}


===== package.json =====
{
  "name": "tanstack_start_ts",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "overrides": {
    "rolldown": "1.2.1"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.2.2",
    "@radix-ui/react-accordion": "^1.2.12",
    "@radix-ui/react-alert-dialog": "^1.1.15",
    "@radix-ui/react-aspect-ratio": "^1.1.8",
    "@radix-ui/react-avatar": "^1.1.11",
    "@radix-ui/react-checkbox": "^1.3.3",
    "@radix-ui/react-collapsible": "^1.1.12",
    "@radix-ui/react-context-menu": "^2.2.16",
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-dropdown-menu": "^2.1.16",
    "@radix-ui/react-hover-card": "^1.1.15",
    "@radix-ui/react-label": "^2.1.8",
    "@radix-ui/react-menubar": "^1.1.16",
    "@radix-ui/react-navigation-menu": "^1.2.14",
    "@radix-ui/react-popover": "^1.1.15",
    "@radix-ui/react-progress": "^1.1.8",
    "@radix-ui/react-radio-group": "^1.3.8",
    "@radix-ui/react-scroll-area": "^1.2.10",
    "@radix-ui/react-select": "^2.2.6",
    "@radix-ui/react-separator": "^1.1.8",
    "@radix-ui/react-slider": "^1.3.6",
    "@radix-ui/react-slot": "^1.2.4",
    "@radix-ui/react-switch": "^1.2.6",
    "@radix-ui/react-tabs": "^1.1.13",
    "@radix-ui/react-toggle": "^1.1.10",
    "@radix-ui/react-toggle-group": "^1.1.11",
    "@radix-ui/react-tooltip": "^1.2.8",
    "@tailwindcss/vite": "^4.2.1",
    "@tanstack/react-query": "^5.101.1",
    "@tanstack/react-router": "1.170.18",
    "@tanstack/react-start": "1.168.32",
    "@tanstack/router-plugin": "1.168.23",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "cmdk": "^1.1.1",
    "date-fns": "^4.1.0",
    "embla-carousel-react": "^8.6.0",
    "input-otp": "^1.4.2",
    "lucide-react": "^0.575.0",
    "react": "^19.2.0",
    "react-day-picker": "^9.14.0",
    "react-dom": "^19.2.0",
    "react-hook-form": "^7.71.2",
    "react-resizable-panels": "^4.6.5",
    "recharts": "^2.15.4",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.5.0",
    "tailwindcss": "^4.2.1",
    "tw-animate-css": "^1.3.4",
    "vaul": "^1.1.2",
    "vite-tsconfig-paths": "^6.0.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@lovable.dev/vite-tanstack-config": "^2.20.0",
    "@types/node": "^22.16.5",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.2.0",
    "eslint": "^9.32.0",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-prettier": "^5.2.6",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^15.15.0",
    "nitro": "3.0.260603-beta",
    "prettier": "^3.7.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.56.1",
    "vite": "8.1.5"
  }
}


===== vite.config.ts =====
// @lovable.dev/vite-tanstack-config already includes the following — do NOT add them manually
// or the app will break with duplicate plugins:
//   - TanStack devtools (dev-only, first), tanstackStart, viteReact, tailwindcss, tsConfigPaths,
//     nitro (build-only using cloudflare as a default target), VITE_* env injection, @ path alias,
//     React/TanStack dedupe, error logger plugins, and sandbox detection (port/host/strictPort).
// You can pass additional config via defineConfig({ vite: { ... }, etc... }) if needed.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({
  tanstackStart: {
    // Redirect TanStack Start's bundled server entry to src/server.ts (our SSR error wrapper).
    // nitro/vite builds from this
    server: { entry: "server" },
  },
});


===== tsconfig.json =====
{
  "include": ["src/**/*.ts", "src/**/*.tsx", "vite.config.ts", "eslint.config.js"],
  "compilerOptions": {
    "target": "ES2022",
    "jsx": "react-jsx",
    "module": "ESNext",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],

    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": false,
    "noEmit": true,

    "skipLibCheck": true,
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noPropertyAccessFromIndexSignature": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedSideEffectImports": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
